# Grafana data source provisioning — added 2026-09-16 with the Loki/Alloy stack.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: loki
    url: http://loki:3100
    isDefault: false
    editable: true
    jsonData:
      maxLines: 2000
      timeout: 60
